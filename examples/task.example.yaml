id: TASK-2026-03-24-sf-owner-regression
title: sf-owner-regression
stage: investigate
question: Investigate why Salesforce owner mapping regresses on ACE update events
symptoms:
  - Existing opportunities switch owner after Partner Central updates
  - Regressions appear after event replay
scope:
  files:
    - src/sync/owner_mapper.ts
    - src/sync/events.ts
    - tests/owner_mapper.test.ts
  logs:
    - logs/sf-owner-regression.log
  urls: []
constraints:
  - do not scan the entire repo
  - do not modify code during investigation
  - stay within listed files unless the scope is updated intentionally
expected_output:
  - ranked likely root causes
  - smallest next action
stop_condition: Produce top 3 hypotheses and the next validation step.
tool_policy:
  tier: 3
  allowed_tools:
    - aider
  fallback_tools: []
  max_files: 5
  max_iterations: 1
verification:
  commands:
    - npm test -- owner_mapper
repo_url: git@github.com:owner/repo.git
repo_slug: owner__repo
branch: main
workspace_branch: ai-task/2026-03-24-sf-owner-regression
created_at: 2026-03-24T12:00:00
